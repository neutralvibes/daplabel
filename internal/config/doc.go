// Package config implements SPECIFICATION.md §5 configuration loading and precedence (CLI > environment > user config file > system config file > defaults). Not yet ported from src/lib/core.sh's lbl_load_config.
package config
